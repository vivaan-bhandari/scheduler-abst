{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && python -m pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn abst.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/api/",
    "healthcheckTimeout": 100
  }
} 