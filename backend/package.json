{
  "name": "abst-backend",
  "version": "1.0.0",
  "description": "Django backend for ABST application",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "gunicorn abst.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
  }
} 